PROJECT: NOTICIARIO

IMPORTANT:
Keep this file updated after every major branch or feature.

Goal:
Noticiario is a Laravel 12 + Inertia + React + TypeScript application for creating short digital news bulletins. The platform helps editors collect and organize news by location/topic, prepare editions, and build scripts. Future phases may include AI-assisted planning, audio/video generation, and publishing automation.

Architecture:
- Laravel 12
- Inertia
- React
- TypeScript
- Tailwind
- MySQL
- Spatie Laravel Permission
- No Blade application pages
- resources/views/app.blade.php is only the Inertia root view
- React pages live in resources/js/Pages
- React layouts live in resources/js/Layouts

Panels:
Admin:
- URL: /admin
- Permission: admin.access
- Purpose: technical/system administration
- Modules:
  - Dashboard
  - Users
  - Roles
  - Permissions
  - Languages
  - AI Providers (implemented)
  - Impersonation/Login as user

Editor:
- URL: /editor
- Permission: editor.access
- Purpose: main editorial workspace
- Modules:
  - Dashboard
  - News Items
  - News Sources
  - News Categories
  - Locations
  - Editions
  - Scripts
  - Script Builder
  - Editorial Templates
  - AI Prompt Templates
  - Editorial Requests
  - Audio/Video/Publishing placeholders for future phases

Viewer:
- URL: /viewer
- Permission: viewer.access
- Purpose: read-only area
- Modules:
  - Dashboard
  - Published Content

Auth and permissions:
- Login redirects by permission:
  - admin.access -> /admin
  - editor.access -> /editor
  - viewer.access -> /viewer
- Editors must not access /admin
- Viewers must not access /admin or /editor
- Admin impersonation exists for support/testing
- Impersonation is only for admin.access users

Demo users:
- admin@example.com / password
- editor@example.com / password
- viewer@example.com / password

Important commands:
- php artisan migrate
- php artisan db:seed
- php artisan db:seed --class=DemoEditorialSeeder
- php artisan test
- npm run build
- php artisan route:list --name=admin
- php artisan route:list --name=editor
- php artisan route:list --name=viewer
- php artisan route:list --name=locale

Current main entities:
- users
- roles
- permissions
- languages
- locations
- news_categories
- news_category_translations
- news_sources
- news_items
- editions
- edition_news_item
- scripts
- editorial_templates
- ai_providers
- ai_prompt_templates
- editorial_requests
- editorial_request_candidates

Languages:
- languages table configures available languages
- UI locale and editorial language are related but different
- UI locale controls app interface language
- Editorial language controls content language
- Locations can have a default editorial language
- Dates should be displayed with localized formatting
- Do not show seconds in normal editorial UI

Current development decisions:
- Inertia + React app only (no Blade app pages)
- Preserve existing auth/permission rules and panel access
- Preserve admin/editor CRUD behavior and impersonation
- Keep RSS support optional/secondary; not strategic core
- No external API calls in this branch
- No queue/scheduler/audio/video/publishing automation in this branch

What has been done (feature/ui-date-formatting-project-doc):
- Added centralized frontend date helpers in resources/js/lib/dates.ts:
  - formatDate(value, locale?)
  - formatDateTime(value, locale?)
  - formatTime(value, locale?)
  - locale map: en->en-GB, es->es-ES, fr->fr-FR
  - safe fallback for null/invalid values
  - no seconds in default listings
- Added useDateFormatter hook in resources/js/lib/useDateFormatter.ts to read page.props.i18n.locale
- Applied localized datetime display across key Admin/Editor listing & detail pages
- Added datetime-local normalization helper (toDateTimeLocalInputValue) and used it in edit forms where backend timestamps may include seconds/space format
- Updated frontend translation keys for date-time labels
- Added this root context file: doc.txt

In progress:
- Keep date formatting adoption consistent for newly added pages/modules.

Current next priorities:
1. Finish manual AI schedule workflow.
2. Improve prompt quality and template rendering.
3. Add real AI provider integration later.
4. Add source verification/fact-checking before automatic generation.
5. Add automatic scheduler/queues only after manual workflow is stable.
6. Add audio/video/publishing later.

Strategic decision:
RSS is not the main product direction.
RSS may remain optional/secondary, but main value should be AI-assisted editorial planning and automatic noticiario creation.

Main target workflow:
Editorial Request
-> AI/Web Research Brief
-> Candidate News Items
-> Edition
-> Script Builder / AI Script Draft
-> Human Review
-> Audio
-> Video
-> Publishing

EDITORIAL SCHEDULES AND MANUAL AI WORKFLOW

New database tables:
- editorial_schedules
  - Defines reusable schedule configurations (daily/weekly/once/custom), editorial context, language/location/category, timing, manual AI mode, and instruction blocks.
- editorial_schedule_runs
  - Represents concrete workflow executions created from schedules and linked to editions/scripts.

Purpose:
- Schedules let editors configure recurring and one-off noticiario planning templates.
- Runs provide the operational unit for each execution cycle (prompt generation, manual AI response capture, and script draft creation).

Manual AI mode workflow:
1. Configure schedule.
2. Create run.
3. Generate prompt.
4. Copy prompt to external AI.
5. Paste AI response.
6. Create script draft.

Demo schedule seeds added:
- Spain Morning Briefing (08:00 Europe/Madrid)
- Spain Afternoon Briefing (15:00 Europe/Madrid)
- Spain Night Recap (21:00 Europe/Madrid)
- Madrid Local Morning (weekdays at 09:00 Europe/Madrid)
- Spain Sports Evening (20:00 Europe/Madrid)
- Super Bowl Special Demo (once, America/New_York)

New routes/pages:
- Editor routes:
  - /editor/editorial-desk
  - /editor/editorial-schedules (CRUD)
  - /editor/editorial-schedule-runs (index/show)
  - run workflow actions:
    - create run
    - generate prompt
    - receive response
    - create script
    - complete run
- React pages:
  - resources/js/Pages/Editor/EditorialDesk/Index.tsx
  - resources/js/Pages/Editor/EditorialSchedules/*
  - resources/js/Pages/Editor/EditorialScheduleRuns/*

Current limitations:
- No automatic AI calls yet.
- No cron/scheduler execution yet.
- No queue workers yet.
- No audio/video/publishing automation yet.

Conventions and restrictions:
- Do not create Blade application pages.
- Do not use migrate:fresh on development DB.
- Do not wipe/truncate developer data.
- Do not expose API keys.
- Do not store real API keys in database.
- Use TypeScript/React pages for app UI.
- Use Tailwind for styling.
- Use centralized helpers for common formatting.
- Preserve existing routes unless strictly necessary.


EDITORIAL SCHEDULES, RUNS AND DASHBOARDS

Database:
- editorial_schedules: configurable recurring and one-off planning definitions with location/category/language/tone/timing and manual AI instructions.
- editorial_schedule_runs: concrete executions linked to schedule, edition, optional request and script, with status lifecycle and prompt/response snapshots.

Purpose:
- Schedules define what should run and how the bulletin should be produced.
- Runs track the operational lifecycle for each execution and maintain audit/context metadata.

Manual AI mode:
- Prompt is generated by the platform and copied manually into external AI tools.
- Response is pasted manually back into the platform.
- Platform converts response into draft script linked to edition/run.

Dashboards:
- Editor dashboard now acts as the operational desk overview with today's tasks and action queues.
- Editorial Desk page provides a focused expanded workflow board.
- Admin dashboard provides system/technical overview plus editorial health indicators.
- Viewer dashboard provides read-only overview of completed scripts/runs and upcoming editions.

Workflow:
1. Configure schedule.
2. Create run.
3. Generate prompt.
4. Copy prompt to external AI.
5. Paste response.
6. Create script draft.

Demo data:
- Demo schedules include Spain 08:00/15:00/21:00, Madrid weekdays 09:00, Spain sports 20:00, and Super Bowl special.
- Demo runs include pending/prompt-ready/response-received/script-created examples for dashboard visibility.

Routes/pages:
- editor.dashboard (operational overview)
- editor.editorial-desk.index
- editor.editorial-schedules.*
- editor.editorial-schedule-runs.index/show + action routes
- admin.dashboard (system + editorial overview)
- viewer.dashboard (read-only overview)

Current limitations:
- No automatic AI calls yet.
- No cron/scheduler yet.
- No queue workers yet.
- No audio/video/publishing automation yet.

PROMPT QUALITY AND AI RESPONSE PARSING

What was improved:
- Editorial schedule run prompt generation now includes stronger guidance for factuality, recency, source hints, uncertainty handling, short presenter-ready paragraphs, and strict response structure.
- The default expected external AI response format is now structured plain text:
  - TITLE
  - INTRO
  - NEWS ITEMS with repeated HEADLINE/SUMMARY/SCRIPT/EDITORIAL ANGLE/SOURCE HINTS blocks
  - OUTRO
  - NOTES
- Prompt language instruction now explicitly tells the external AI to answer in the configured schedule language.

AiResponseParser service:
- New service: app/Services/EditorialScheduling/AiResponseParser.php
- Method: parse(string $responseText): array
- Handles English and Spanish label variants, including:
  - TITLE / TÍTULO
  - INTRO / INTRODUCTION / ENTRADILLA
  - NEWS ITEMS / ITEMS / NOTICIAS
  - HEADLINE / TITULAR
  - SUMMARY / RESUMEN
  - SCRIPT / GUION
  - EDITORIAL ANGLE / ENFOQUE EDITORIAL
  - SOURCE HINTS / FUENTES
  - OUTRO / CIERRE
  - NOTES / NOTAS
- Deterministic and tolerant parser behavior.
- Safe fallback for unstructured responses:
  - title=null, intro=null, outro=null, items=[]
  - body=full response text

Storage on editorial_schedule_runs:
- Added nullable JSON fields:
  - parsed_response
  - parser_warnings
- On AI response save:
  - ai_response_text is stored
  - response is parsed and stored in parsed_response
  - warnings are saved when structure is missing
  - response_received_at is set
  - status becomes response_received

Script creation from parsed response:
- If parsed_response contains useful structure:
  - script.title uses parsed title (fallback to edition title)
  - script.intro/outro are filled from parsed content
  - script.body is assembled from parsed items for presenter-ready flow
  - metadata includes parsed_response_used, news_item_count, and flattened source_hints
- If parsing is weak/unstructured:
  - fallback to previous behavior with full ai_response_text in body
  - parsed_response_used=false
- Existing script duplication is avoided (if script already exists for run, it is reused).

Editorial Run UI preview:
- Run show page now includes parsed response preview:
  - parsed title/intro/body/outro/notes
  - parsed items with headline/summary/script/editorial angle/source hints
  - parser warnings
  - raw response fallback preview
- Includes clear fallback messaging when no structured sections are detected.

Prompt template seeds:
- Updated seeded templates using updateOrCreate without deleting existing templates:
  - Manual AI Research And Script Prompt
  - Spanish Noticiario Prompt
  - Sports Special Prompt
- expected_output_format updated to structured_text for these templates.

Demo data updates:
- Demo schedule run seeding now includes a generated prompt snapshot, structured sample ai_response_text, and parsed_response snapshot for a manual workflow example.
- Seeding remains idempotent via updateOrCreate.

Optional extraction of candidate news items:
- Not implemented in this branch to keep scope safe and preserve current CRUD stability.
- Next incremental step: add manual action to create News Items from parsed_response with duplicate guard.

Current limitations:
- no real AI API calls yet
- no automatic fact-checking yet
- no source verification yet
- no audio/video/publishing yet

Current next priorities:
1. Improve/manual test prompt quality with real external AI tools.
2. Add source verification/fact-checking workflow.
3. Add optional real AI provider integration.
4. Add automatic schedule execution later.
5. Add audio/video/publishing after editorial workflow is stable.

SEO AND TRACKING SETTINGS

Database:
- Added seo_settings table for global SEO and tracking configuration.
- This table is intended to keep one active/global row.

Admin module:
- Added Admin > SEO & Tracking page:
  - Route: /admin/seo-settings
  - Permission: admin.access only
- Editors/viewers cannot access this module.

Configurable SEO defaults:
- site_name
- default_title
- title_suffix
- default_description
- default_keywords
- canonical_base_url
- default_robots
- default_og_image
- default_twitter_card
- enable_indexing

Search verification tokens:
- google_site_verification
- bing_site_verification

Tracking IDs:
- google_tag_manager_id
- google_analytics_id
- microsoft_clarity_id
- meta_pixel_id
- tiktok_pixel_id

Custom script injection:
- custom_head_scripts
- custom_body_start_scripts
- custom_body_end_scripts
- enable_custom_scripts toggle

Safety defaults and guardrails:
- Only admin.access can manage SEO/tracking settings.
- Tracking is disabled by default.
- Custom scripts are disabled by default.
- Do not store private API keys in this module.

Injection location:
- SEO/meta/tracking/custom scripts are injected from resources/views/app.blade.php.
- This file is the Inertia root view (not a Blade application page).

Operational notes:
- The app safely handles missing seo_settings table during fresh installs/tests.
- Demo seeders create/update one safe default SEO row with tracking/custom scripts disabled.

Future considerations:
- Cookie consent / privacy compliance
- GDPR tracking consent
- CSP/security headers
- Per-environment tracking configuration
- Page-specific SEO overrides

Current modules list update:
- Admin modules now include SEO & Tracking configuration.

Current next priorities update:
1. Stabilize editorial schedule + run workflow with stronger QA.
2. Add page-level SEO override strategy for public content pages.
3. Add privacy consent framework before enabling production tracking by default.
4. Continue improving prompt/template quality and editorial automation reliability.

MANUAL FACT-CHECK AND SCRIPT REVIEW

Overview:
- Added a manual editorial review and fact-check layer on top of existing script status flow.
- This workflow is designed for human review before any automated AI/audio/video/publishing steps.

Scripts review fields:
- Added review_status (default: pending).
- Added review_notes and fact_check_notes.
- Added reviewed_by/reviewed_at.
- Added rejected_by/rejected_at/rejection_reason.
- Kept existing status + approved_by/approved_at behavior and used review_status as a parallel editorial layer.

script_review_items table:
- New granular review table linked to scripts.
- Supports optional link to news_items.
- Stores type, content, source_hints, verification_status, verification_notes, required_action, reviewer/timestamp, and metadata.

Purpose of review items:
- Allow editors to review each script section independently.
- Support fact-check and source completeness checks at block/item level.
- Enable clear manual workflow before final script approval.

Review statuses:
- pending
- in_review
- needs_sources
- needs_changes
- verified
- approved
- rejected

Verification statuses:
- pending
- verified
- needs_source
- needs_changes
- rejected
- not_applicable

Required actions:
- add_source
- rewrite
- remove
- check_date
- check_claim
- none

ScriptReviewItemGenerator service:
- New service: app/Services/EditorialReview/ScriptReviewItemGenerator.php
- Deterministically generates review items without AI/external calls.
- Prefers parsed metadata items when available.
- Otherwise splits body by double new lines and falls back safely.
- Optionally links edition news items to review items.
- Prevents duplicate generation unless force regeneration is requested.

Script Review page:
- New page: Editor/Scripts/Review
- Includes script header/status summary, counters, script-level review notes, per-item verification controls, and workflow actions.
- Supports generate items, mark in review, mark verified, approve, and reject (with required reason).

Approval/rejection workflow:
- Verified only when unresolved blocking item states are absent.
- Approval allowed when review_status=verified or all items are verified/not_applicable.
- Rejection requires rejection_reason.
- Approval/rejection also updates legacy script status to preserve previous workflow compatibility.

Relationship with AI-generated/manual scripts:
- Works for manually written scripts and scripts created from manual AI response parsing.
- No AI call is required for review item generation.

Current limitations:
- no automatic fact-checking yet
- no external source verification yet
- no AI review yet
- no audio/video/publishing yet

Current next priorities:
1. Improve review workflow based on manual testing.
2. Add real source verification/fact-checking support.
3. Add optional real AI provider integration.
4. Add automatic schedule execution later.
5. Add audio/video/publishing after editorial workflow is stable.

SOURCE VERIFICATION WORKFLOW

Overview:
- Added a manual source verification workflow for scripts, script review items, and news items.
- This workflow is fully manual: no external calls, no scraping, and no automatic URL verification.

source_references table:
- New table: source_references
- Stores editor-managed source references tied to:
  - news_items (optional)
  - scripts (optional)
  - script_review_items (optional)
  - editorial_schedule_runs (optional)
- Includes title, source_name, source_url, source_type, verification_status, trust_level, checked_by/checked_at, notes, metadata, timestamps, and soft deletes.

Purpose of source references:
- Track the specific references editors use to validate claims.
- Make source quality visible during script review and approval.
- Surface weak/missing/broken/rejected references before approval.

Source types:
- web
- official
- agency
- social
- rss
- manual
- unknown

Verification statuses:
- pending
- verified
- weak
- missing
- broken
- rejected
- not_required

Trust level:
- Numeric editorial trust score (manual).
- Intended for desk-level confidence scoring and prioritization.

Model relations:
- Script hasMany sourceReferences.
- ScriptReviewItem hasMany sourceReferences.
- NewsItem hasMany sourceReferences.
- EditorialScheduleRun hasMany sourceReferences.
- SourceReference belongsTo checkedBy user.

SourceReferenceExtractor service:
- New service: app/Services/EditorialReview/SourceReferenceExtractor.php
- Extracts source hints from:
  - script metadata and text
  - script review item source_hints/content
  - parsed response structures and source sections (SOURCE HINTS, FUENTES, SOURCES)
- Extracts URLs via regex.
- Deduplicates safe re-runs and avoids duplicate inserts.

Source References pages:
- Editor/SourceReferences/Index
- Editor/SourceReferences/Show
- Supports filtering, listing, detail/edit form, verification updates, and linked content navigation.

Script Review integration:
- Review page now shows linked source references per review item.
- Added extract source actions for scripts and review items.
- Approval now blocks when source references include missing/broken/rejected states.

Script Show and News Item integration:
- Script show includes source summary and quick actions to extract/manage sources.
- News item show surfaces source references and allows creating a source reference from source_url.

Dashboard integration:
- Added source verification cards:
  - Sources pending verification
  - Weak sources
  - Missing sources
  - Broken/rejected sources
  - Scripts blocked by sources

Current limitations:
- no automatic URL checking
- no scraping
- no external API calls
- no automatic fact-checking yet

Current next priorities:
1. Improve source verification workflow based on manual testing.
2. Add optional live URL checking later.
3. Add real AI provider integration.
4. Add automatic schedule execution later.
5. Add audio/video/publishing after editorial workflow is stable.

USER PREFERENCES AND AVATAR

Overview:
- Added user profile preference fields and avatar support.
- Preferences now support preferred locale, timezone, and date/time display preferences.

User fields:
- preferred_locale (nullable)
- timezone (nullable)
- avatar_path (nullable)
- date_format (nullable)
- time_format (nullable)

Locale resolution order:
1. session locale
2. authenticated user preferred_locale
3. app/default locale

Profile page updates:
- Profile now supports editing:
  - name
  - email
  - preferred locale
  - timezone
  - date format
  - time format
  - avatar upload (jpg/jpeg/png/webp, max 2MB)
- If avatar is not set, the UI uses initials fallback.

Admin user management updates:
- Admin user list now displays avatar/initials, preferred locale, and timezone.
- Admin create/edit supports preferred locale, timezone, date/time format preferences, and optional avatar upload.

Layout avatar display:
- Admin, Editor, and Viewer layouts now display user avatar or initials in the top bar.
- Works with impersonation context because top-bar user data is shared through auth props.

Storage note:
- Avatar URLs are served from public storage.
- Run: php artisan storage:link

SOURCE VERIFICATION WORKFLOW

Current limitations (confirmed):
- no automatic URL checking
- no scraping
- no external API calls
- no automatic fact-checking yet

Current next priorities:
1. Improve source verification workflow based on manual testing.
2. Add optional live URL checking later.
3. Add real AI provider integration.
4. Add AI request logs and cost control.
5. Add automatic schedule execution later.
6. Add audio/video/publishing after editorial workflow is stable.

USER MANAGEMENT

Admin Users CRUD:
- Admin users listing includes avatar/initials, name, email, role badges, preferred locale, timezone, status, created date, and actions for edit/impersonate/delete.
- Admin Users Edit page uses Inertia React form with proper method spoofing and multipart FormData support, fixing missing name/email on save.
- Admin Users Create/Edit support:
  - name
  - email
  - password / confirm password
  - roles
  - direct permissions
  - preferred_locale
  - timezone
  - date_format
  - time_format
  - avatar upload
  - active status
- Role helper text is shown in Create/Edit UI:
  - superadmin: full system access
  - admin: technical administration and access to editor/viewer panels
  - editor: editorial workspace access
  - viewer: read-only panel access

User preference fields:
- preferred_locale
- timezone
- avatar_path
- date_format
- time_format

Role management from admin:
- Admin can update user roles from user edit/create pages.
- Safety rule added: avoid removing last admin-like access (admin/superadmin/super-admin) from the final admin-like user.

Basic roles and access:
- Roles ensured via seeder (idempotent):
  - superadmin
  - admin
  - editor
  - viewer
- Core panel permissions:
  - admin.access
  - editor.access
  - viewer.access
- Role access mapping:
  - superadmin/admin -> admin/editor/viewer
  - editor -> editor
  - viewer -> viewer
- Legacy role compatibility kept for existing super-admin naming.

Impersonation/login as user:
- Preserved existing admin impersonation behavior and routes.

Current next priorities:
1. Finish/fix user management and preferences.
2. Continue source verification workflow if not complete.
3. Add real AI provider integration.
4. Add AI run logs and cost control.
5. Add automatic schedule execution later.
6. Add audio/video/publishing after editorial workflow is stable.
