import AdminLayout from '@/Layouts/AdminLayout'; import { Head, useForm } from '@inertiajs/react'; import { useTranslations } from '@/i18n/useTranslations';
export default function Edit({setting}:any){ const {t}=useTranslations(); const form=useForm({...setting,email_recipients:(setting.email_recipients||[]).join(', ')}); return <AdminLayout><Head title={t('Edit')} /><form onSubmit={(e)=>{e.preventDefault(); form.put(route('admin.operation-alert-settings.update',setting.id));}} className='space-y-2'><input className='w-full border p-2' value={form.data.name} onChange={e=>form.setData('name',e.target.value)} /><input className='w-full border p-2' value={form.data.email_recipients} onChange={e=>form.setData('email_recipients',e.target.value)} /><button className='rounded bg-slate-900 px-3 py-2 text-white'>{t('Save')}</button></form></AdminLayout>}
