import AdminLayout from '@/Layouts/AdminLayout'; import { Head, Link } from '@inertiajs/react'; import { useTranslations } from '@/i18n/useTranslations';
export default function Index({settings}:any){ const {t}=useTranslations(); return <AdminLayout><Head title={t('Operation Alert Settings')} /><h1 className='mb-4 text-2xl font-bold'>{t('Operation Alert Settings')}</h1>{settings.data.map((s:any)=><div key={s.id} className='mb-2 rounded border p-2'>{s.name} <Link className='text-cyan-700' href={route('admin.operation-alert-settings.edit',s.id)}>{t('Edit')}</Link></div>)}</AdminLayout>}
