import AdminLayout from '@/Layouts/AdminLayout'; import { Head, Link, router } from '@inertiajs/react'; import { useTranslations } from '@/i18n/useTranslations'; import { useEffect } from 'react';
export default function Index({ summary, recentEvents }: any){ const {t}=useTranslations(); useEffect(()=>{ const id=setInterval(()=>router.reload({ only:['summary','recentEvents'] }),30000); return ()=>clearInterval(id); },[]);
return <AdminLayout><Head title={t('Operations Dashboard')} /><div className='space-y-4'><h1 className='text-2xl font-bold'>{t('Operations Dashboard')}</h1><p>{t('This monitoring dashboard uses polling')}</p><div className='grid grid-cols-2 gap-3 md:grid-cols-4'>{Object.entries(summary).map(([k,v])=><div key={k} className='rounded border p-3'><div className='text-xs'>{t(k.replaceAll('_',' '))}</div><div className='text-xl font-semibold'>{String(v)}</div></div>)}</div><Link className='text-cyan-700' href={route('admin.operations.events.index')}>{t('Operational Events')}</Link><div><h2 className='font-semibold'>{t('Recent operational events')}</h2>{recentEvents.map((e:any)=><div key={e.id} className='border-b py-2 text-sm'>{e.title} - {e.severity}</div>)}</div></div></AdminLayout>}
