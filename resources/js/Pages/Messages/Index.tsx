import { Button } from '@/Components/ui/button'; import { Card, CardContent, CardHeader, CardTitle } from '@/Components/ui/card'; import { useTranslations } from '@/i18n/useTranslations'; import EditorLayout from '@/Layouts/EditorLayout'; import { Head, Link, router } from '@inertiajs/react';
export default function Index({ messages }: any){ const {t}=useTranslations(); return <EditorLayout><Head title={t('Messages')} /><Card><CardHeader><CardTitle>{t('Inbox')}</CardTitle></CardHeader><CardContent className='space-y-3'>{messages.data.map((m:any)=><div key={m.id} className='rounded border p-3'><Link className='font-semibold underline' href={route('messages.show',m.id)}>{m.subject}</Link><p className='text-xs'>{m.message_type} · {m.created_at}</p><div className='mt-2 flex gap-2'><Button size='sm' onClick={()=>router.post(route('messages.mark-read',m.id))}>{t('Mark as read')}</Button><Button size='sm' variant='outline' onClick={()=>router.post(route('messages.archive',m.id))}>{t('Archive message')}</Button></div></div>)}</CardContent></Card></EditorLayout>}
