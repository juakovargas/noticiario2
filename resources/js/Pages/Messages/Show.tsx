import { Card, CardContent, CardHeader, CardTitle } from '@/Components/ui/card'; import { useTranslations } from '@/i18n/useTranslations'; import EditorLayout from '@/Layouts/EditorLayout'; import { Head, Link } from '@inertiajs/react';
export default function Show({ message }: any){ const {t}=useTranslations(); return <EditorLayout><Head title={t('Message')} /><Card><CardHeader><CardTitle>{message.subject}</CardTitle></CardHeader><CardContent><p className='text-xs'>{t('Sender')}: {message.sender?.name ?? '-'}</p><p className='mt-4 whitespace-pre-wrap'>{message.body}</p><Link className='mt-4 inline-block underline' href={route('messages.index')}>{t('Open messages')}</Link></CardContent></Card></EditorLayout> }
